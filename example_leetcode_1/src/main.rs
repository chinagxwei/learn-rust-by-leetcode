use example_leetcode_1::{two_sum, add_two_numbers, ListNode, length_of_longest_substring, find_median_sorted_arrays};
use std::borrow::Borrow;

fn main() {

//    let n = length_of_longest_substring(String::from("abcabcbb"));
//    let m = 3.max(2);
//    let s = "abcdefg".to_string();
//    let c = s.get(3..=3);
//    let s = [1,2,3];
//    println!("{:?}",&s[0..])
    find_median_sorted_arrays([1, 2, 3,4].to_vec(), [4, 5].to_vec());
}
